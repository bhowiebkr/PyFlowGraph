# .github/workflows/windows-build.yml
# GitHub Actions workflow to build a standalone Windows executable and create a release.
# Now correctly prepares the bundled Python runtime to support venv and pip.

name: Build and Release Windows App

on:
  push:
    tags:
      - 'v*' # Triggers on version tags like v1.0.0

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write      # To create the release
      pull-requests: read  # To read PRs for the changelog

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python for build process
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Prepare Portable Python Runtime
        shell: pwsh
        run: |
          # 1. Download and extract the embeddable Python package
          Invoke-WebRequest -Uri "https://www.python.org/ftp/python/3.11.5/python-3.11.5-embed-amd64.zip" -OutFile "python-embed.zip"
          Expand-Archive -Path "python-embed.zip" -DestinationPath "python_runtime"
          
          # 2. Unzip the standard library into a 'Lib' folder
          Expand-Archive -Path "python_runtime\python311.zip" -DestinationPath "python_runtime\Lib"
          Remove-Item "python_runtime\python311.zip"

          # 3. Download the official pip bootstrap script
          Invoke-WebRequest -Uri "https://bootstrap.pypa.io/get-pip.py" -OutFile "python_runtime\get-pip.py"
          
          # 4. CRITICAL FIX: Modify the ._pth file to point to the unzipped Lib directory
          # and enable site-packages. This allows Python to find its standard library and pip.
          $pth_file = "python_runtime\python311._pth"
          (Get-Content $pth_file) -replace 'python311.zip', 'Lib' -replace '#import site', 'import site' | Set-Content $pth_file
          
          # 5. Run the bootstrap script to install pip into the embedded runtime
          .\python_runtime\python.exe .\python_runtime\get-pip.py
          
          # 6. Clean up the bootstrap script
          Remove-Item "python_runtime\get-pip.py"

      - name: Cache Pip and Nuitka
        uses: actions/cache@v4
        with:
          path: |
            ~\.cache\pip
            ~\AppData\Local\Nuitka\Nuitka\Cache
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build app with Nuitka
        run: |
          python -m nuitka `
            --standalone `
            --enable-plugin=pyside6 `
            --include-qt-plugins=platforms `
            --output-dir=NodeEditor_Build `
            --nofollow-import-to=tkinter,unittest,setuptools,pip,wheel `
            --windows-disable-console `
            --remove-output `
            --lto=yes `
            --include-data-dir=examples=examples `
            --include-data-file=dark_theme.qss=dark_theme.qss `
            --include-data-dir=python_runtime=python_runtime `
            --assume-yes-for-downloads `
            main.py

      - name: Prepare artifact for release
        id: package
        run: |
          $version = "${{ github.ref_name }}"
          $build_dir = "NodeEditor_Build"
          $dist_dir = Join-Path $build_dir "main.dist"
          
          $new_dir_name = "NodeEditor $version"
          $zip_file_name = "NodeEditor_Windows_$version.zip"
          
          Rename-Item -Path $dist_dir -NewName $new_dir_name
          
          $archive_source = Join-Path $build_dir $new_dir_name
          Compress-Archive -Path "$archive_source\*" -DestinationPath $zip_file_name
          
          echo "zip_name=$zip_file_name" >> $env:GITHUB_OUTPUT

      - name: Generate changelog
        id: changelog
        uses: mikepenz/release-changelog-builder-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body: |
            ## What's Changed
            ---

            ${{ steps.changelog.outputs.changelog }}
          files: ${{ steps.package.outputs.zip_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
